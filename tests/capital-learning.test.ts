import assert from"node:assert/strict";import{capitalEfficiency,synthesizeCapitalLearning}from"../src/lib/capital-learning";const a={id:"a",category:"Technology",spentAmount:100,outcomeValue:180,roi:80,createdAt:"2026-01-01",closedAt:"2026-01-11",learning:"Automation improved throughput",milestone:"velocity"};const b={id:"b",category:"Market",spentAmount:100,outcomeValue:90,roi:-10,createdAt:"2026-01-01",closedAt:"2026-02-10",learning:"Channel underperformed",milestone:"revenue"};assert.ok(capitalEfficiency(a).score>capitalEfficiency(b).score);const r=synthesizeCapitalLearning([a,b]);assert.equal(r.status,"AVAILABLE");assert.equal(r.categories.length,2);assert.equal(r.reallocation?.to,"Technology");assert.equal(r.reallocation?.from,"Market");assert.equal(r.confidence,"LOW");const empty=synthesizeCapitalLearning([]);assert.equal(empty.status,"INSUFFICIENT_DATA");console.log("Capital Learning Intelligence tests passed");